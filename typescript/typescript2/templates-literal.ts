(function(){

 
    const nombre = 'Jorge';
    const apellido = 'Soriano';
    const edad = 26;

    //const salida = nombre + " " + apellido + " ( " + edad + " ) ";
   const salida = `${nombre} ${apellido} (${edad+10})`;
    console.log(salida);
     
})();
